#!/bin/sh
# Either runs the TreeSetAnalyser from the trunk directory, or
# otherwise the SNAP environment veriable should point there.
SNAP=$SNAP./dist
java -Xms64m -Xmx256m -cp $SNAP/snap.jar snap.util.TreeSetAnalyser $*

